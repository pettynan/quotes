# quotes
Project by Peter Tynan and Anthony Berry

## Instructions for Running Code
To run code, cd into top level of Quotes directory and run "./gradlew run" in the command line. You should see a random quote (with its author!) returned in the command line!!

## Methods
  Quote Class
  - Quote toString()
  getQuoteData()
  jsonToQuote()
  printRandomObject()